---
name: speckit-tasks
description: Run the repository's canonical Spec Kit task-generation workflow in Codex
---

# Spec Kit Tasks

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.tasks.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
