---
name: speckit-specify
description: Run the repository's canonical Spec Kit specification workflow in Codex
---

# Spec Kit Specify

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.specify.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
