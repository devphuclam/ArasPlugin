---
name: speckit-implement
description: Run the repository's canonical Spec Kit implementation workflow in Codex
---

# Spec Kit Implement

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.implement.md` completely. Execute that command's
workflow in the current Codex turn, honoring its writer/reviewer and evidence
gate rules. `$ARGUMENTS` means the user's current request.
