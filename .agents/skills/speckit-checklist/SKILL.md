---
name: speckit-checklist
description: Run the repository's canonical Spec Kit checklist workflow in Codex
---

# Spec Kit Checklist

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.checklist.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
