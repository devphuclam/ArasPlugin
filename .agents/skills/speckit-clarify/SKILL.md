---
name: speckit-clarify
description: Run the repository's canonical Spec Kit clarification workflow in Codex
---

# Spec Kit Clarify

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.clarify.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
