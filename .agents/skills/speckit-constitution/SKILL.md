---
name: speckit-constitution
description: Run the repository's canonical Spec Kit constitution workflow in Codex
---

# Spec Kit Constitution

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.constitution.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
