---
name: speckit-analyze
description: Run the repository's canonical Spec Kit artifact analysis workflow in Codex
---

# Spec Kit Analyze

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.analyze.md` completely. Execute that command's
workflow in the current Codex turn, including its read-only and reporting
rules. `$ARGUMENTS` means the user's current request.
