---
name: speckit-converge
description: Run the repository's canonical Spec Kit convergence workflow in Codex
---

# Spec Kit Converge

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.converge.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
