---
name: speckit-plan
description: Run the repository's canonical Spec Kit planning workflow in Codex
---

# Spec Kit Plan

Read `.agents/skills/SPECKIT_ADAPTER_CONTRACT.md` and then read
`.opencode/commands/speckit.plan.md` completely. Execute that command's
workflow in the current Codex turn. `$ARGUMENTS` means the user's current request.
