<Window x:Class="IdeaCadConnector.Desktop.ReviewDecisionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Review Decision"
        Width="500"
        Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#EEF3FA"
        FontFamily="Segoe UI"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <SolidColorBrush x:Key="DialogTextBrush" Color="#172B55"/>
        <SolidColorBrush x:Key="DialogMutedBrush" Color="#61708E"/>
        <SolidColorBrush x:Key="DialogLineBrush" Color="#D9E3F0"/>
        <SolidColorBrush x:Key="DialogPrimaryBrush" Color="#1E5AF7"/>
        <SolidColorBrush x:Key="DialogSuccessBrush" Color="#067647"/>
        <SolidColorBrush x:Key="DialogWarningBrush" Color="#B54708"/>
        <Style x:Key="DialogLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#28416C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="DialogButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root" Background="White" BorderBrush="#C9D6EA" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#F4F7FC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Margin="18" Background="White" BorderBrush="#DDE6F2" BorderThickness="1" CornerRadius="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="{StaticResource DialogLineBrush}" BorderThickness="0,0,0,1" Padding="26,22,26,18">
                <StackPanel>
                    <TextBlock Text="Review Decision" FontSize="22" FontWeight="Bold" Foreground="{StaticResource DialogTextBrush}"/>
                    <TextBlock Text="Approve or request rework for this submission." Margin="0,4,0,0" Foreground="{StaticResource DialogMutedBrush}" FontSize="13"/>
                </StackPanel>
            </Border>
            <StackPanel Grid.Row="1" Margin="26,22">
                <TextBlock Text="Submission Info" Style="{StaticResource DialogLabelStyle}"/>
                <TextBlock x:Name="SubmissionInfo" Margin="0,4,0,12" Foreground="{StaticResource DialogTextBrush}" FontSize="13" TextWrapping="Wrap"/>
                <TextBlock Text="Comment (required for rework)" Style="{StaticResource DialogLabelStyle}"/>
                <TextBox x:Name="CommentBox" Margin="0,8,0,0" Height="80" Padding="12,8" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" Foreground="{StaticResource DialogTextBrush}" Background="White" BorderBrush="#C9D6EA" BorderThickness="1" FontSize="13"/>
                <Border x:Name="GateNote" Margin="0,12,0,0" Padding="10" Background="#FFF3E3" BorderBrush="#FEDF89" BorderThickness="1" CornerRadius="8" Visibility="Collapsed">
                    <TextBlock x:Name="GateNoteText" Foreground="{StaticResource DialogWarningBrush}" TextWrapping="Wrap" FontSize="12"/>
                </Border>
            </StackPanel>
            <Border Grid.Row="2" Background="#FAFCFF" BorderBrush="{StaticResource DialogLineBrush}" BorderThickness="0,1,0,0" Padding="26,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Width="104" Content="Cancel" IsCancel="True" Style="{StaticResource DialogButtonStyle}"/>
                    <Button x:Name="ReworkButton" Width="140" Margin="10,0,0,0" Content="Request Rework" Click="ReworkButton_Click" Style="{StaticResource DialogButtonStyle}"/>
                    <Button x:Name="ApproveButton" Width="120" Margin="10,0,0,0" Content="Approve" IsDefault="True" Click="ApproveButton_Click" Style="{StaticResource DialogButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
