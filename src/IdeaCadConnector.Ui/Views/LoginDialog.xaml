<Window x:Class="IdeaCadConnector.Ui.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:IdeaCadConnector.Core.Localization;assembly=IdeaCadConnector.Core"
        Title="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[LoginDialogTitle]}"
        Width="1080"
        Height="760"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="100" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[LabelServer]}" />
    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[LabelDatabase]}" />
    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding Database, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[LabelUser]}" />
    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[LabelPassword]}" />
    <PasswordBox x:Name="PasswordInput" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" PasswordChanged="OnPasswordChanged" />

    <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Margin="0,4,0,8" Foreground="Firebrick" Text="{Binding ErrorMessage}" TextWrapping="Wrap" />

    <Border Grid.Row="5" Grid.ColumnSpan="2" Margin="0,0,0,8" Padding="12" BorderBrush="#FFB7C9B9" BorderThickness="1" Background="#FFF7FBF7">
      <StackPanel>
        <TextBlock FontWeight="SemiBold" Text="{Binding ConnectionMessage}" TextWrapping="Wrap" />
        <TextBlock Margin="0,8,0,0" Text="{Binding ConnectionDetails}" TextWrapping="Wrap" />
      </StackPanel>
    </Border>

    <Grid Grid.Row="6" Grid.ColumnSpan="2" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="100" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[LabelPartSearch]}" />
      <TextBox Grid.Column="1" Margin="0,0,8,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" />
      <Button x:Name="SearchButton" Grid.Column="2" Width="120" IsEnabled="False" Click="OnSearchClicked" Content="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ButtonSearchParts]}" />
    </Grid>

    <TextBlock Grid.Row="7" Grid.ColumnSpan="2" Margin="0,0,0,8" Foreground="#FF2C5F2D" Text="{Binding SearchStatusMessage}" TextWrapping="Wrap" />

    <DataGrid x:Name="ResultsGrid"
              Grid.Row="8"
              Grid.ColumnSpan="2"
              Margin="0,0,0,8"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              ItemsSource="{Binding SearchResults}"
              SelectedItem="{Binding SelectedSearchResult, Mode=TwoWay}"
              SelectionMode="Single">
      <DataGrid.Columns>
        <DataGridTextColumn Width="130" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ColumnPartNumber]}" Binding="{Binding Part.PartNumber}" />
        <DataGridTextColumn Width="220" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ColumnPartName]}" Binding="{Binding Part.Name}" />
        <DataGridTextColumn Width="80" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[MetaRevision]}" Binding="{Binding Part.Revision}" />
        <DataGridTextColumn Width="110" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ColumnState]}" Binding="{Binding Part.State}" />
        <DataGridTextColumn Width="120" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ColumnType]}" Binding="{Binding Part.PartType}" />
        <DataGridTextColumn Width="150" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ColumnCadNumber]}" Binding="{Binding IronCadPartCad.CadNumber}" />
        <DataGridCheckBoxColumn Width="95" Header="Has File" Binding="{Binding IronCadPartCad.HasNativeFile}" />
        <DataGridCheckBoxColumn Width="80" Header="Locked" Binding="{Binding IronCadPartCad.IsLocked}" />
        <DataGridTextColumn Width="140" Header="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[MetricLockedBy]}" Binding="{Binding IronCadPartCad.LockedBy}" />
      </DataGrid.Columns>
    </DataGrid>

    <Border Grid.Row="9" Grid.ColumnSpan="2" Margin="0,0,0,8" Padding="12" BorderBrush="#FFD6D6D6" BorderThickness="1" Background="#FFF9F9F9">
      <StackPanel>
        <TextBlock FontWeight="SemiBold" Text="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[SectionSelectedResult]}" />
        <TextBlock Margin="0,8,0,0" FontFamily="Consolas" Text="{Binding SelectedResultDetails}" TextWrapping="Wrap" />
      </StackPanel>
    </Border>

    <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="LoginButton" Width="120" Margin="0,0,8,0" IsDefault="True" Click="OnLoginClicked" Content="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ButtonTestConnection]}" />
      <Button x:Name="RefreshButton" Width="120" Margin="0,0,8,0" IsEnabled="False" Click="OnSearchClicked" Content="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ButtonSearchParts]}" />
      <Button x:Name="CloseButton" Width="88" IsCancel="True" Click="OnCloseClicked" Content="{Binding Source={x:Static core:LocalizationSource.Instance}, Path=[ButtonClose]}" />
    </StackPanel>
  </Grid>
</Window>
